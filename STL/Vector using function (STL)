#include <iostream>
#include<vector>
using namespace std;
void display(vector<int> v1)
{
    for(int i=0;i<v1.size();i++)
    {
        cout<<v1[i]<<" ";
    }
    cout<<endl;
}
int main() {
    vector<int> v;
    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int x;
        cout<<"Enter the value:"<<endl;
        cin>>x;
        v.push_back(x);
    }
    display(v);
    return 0;
}
