#include<iostream>
#include<functional>
#include<algorithm>
using namespace std;
int main()
{
    int arr[]={1,9,4,6,2,3};
    int n=(sizeof(arr)/sizeof(arr[0]));
    sort(arr,arr+n,greater<int>());
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }
}
