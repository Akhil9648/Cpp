#include<iostream>
#include<set>
using namespace std;
void display(set<int> &se)
{
    set<int>::iterator itr;
    for(itr=se.begin();itr!=se.end();itr++)
    {
        cout<<*itr<<" ";
    }
}
int main()
{
    set<int>s;
    s.insert(10);
    s.insert(20);
    s.insert(30);
    s.insert(40);
    s.insert(50);
    s.insert(5);
    s.insert(4);
    display(s);
}
